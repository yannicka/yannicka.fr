# à faire
namespace block.bonus {
	class IncreaseWeaponLatencySpeed : Bonus {
		def new(x double, y double) {
			super(x, y, 16, 16)
		}

		over onPlayerOverlaps(player Player) {
			# à faire
		}
	}
}
